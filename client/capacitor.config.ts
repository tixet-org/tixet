// eslint-disable-next-line import/no-extraneous-dependencies
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iota.tickets',
  appName: 'nft-tickets',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
